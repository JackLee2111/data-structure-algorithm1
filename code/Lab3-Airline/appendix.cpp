/*
struct Queue
{
    VertexNode base[100]; //队列储存的元素
    int rear, front;
    Queue()
    {
        rear = front = 0; //初始化
    }
    void EnQueue(VertexNode EdgeNode);
    VertexNode DeQueue();
    bool empty();
};*/